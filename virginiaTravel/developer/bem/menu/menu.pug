- var navMenuItem = ['Item  1','Item  2', 'Item  3', 'Item  4', 'Item  5'];
mixin menu()
    .menu
        ul.menu_list
            each value in navMenuItem
                li
                    a(href="#").menu_link
                        =value